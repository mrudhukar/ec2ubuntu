1. Clone the repos from git instead of rsync (Steps in Readme), also move the rest of the readme to shell script
2. Comment about the minimum kernel version required for debootstrap lucid
3. Proper installation of ami tools in the image instead of converting rpm package to debian.
